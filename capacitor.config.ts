import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Kairo 4K.streamer',
  appName: 'Kairo 4K Streamer',
  webDir: 'dist'
};

export default config;
